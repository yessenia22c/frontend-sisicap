import { ControlRolesDirective } from './control-roles.directive';

describe('ControlRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlRolesDirective();
    expect(directive).toBeTruthy();
  });
});
